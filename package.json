{
  "name": "nlcst-test",
  "version": "1.0.0",
  "description": "Assert nlcst nodes",
  "license": "MIT",
  "keywords": [
    "assert",
    "test",
    "nlcst",
    "util",
    "utility"
  ],
  "repository": "https://github.com/wooorm/nlcst-test",
  "bugs": "https://github.com/wooorm/nlcst-test/issues",
  "author": "Titus Wormer <tituswormer@gmail.com> (http://wooorm.com)",
  "contributors": [
    "Titus Wormer <tituswormer@gmail.com> (http://wooorm.com)"
  ],
  "files": [
    "index.js"
  ],
  "dependencies": {
    "mapz": "^1.0.0",
    "unist-util-assert": "^1.0.0",
    "zwitch": "^1.0.0"
  },
  "devDependencies": {
    "browserify": "^13.0.1",
    "esmangle": "^1.0.1",
    "nyc": "^9.0.1",
    "remark-cli": "^2.1.0",
    "remark-preset-wooorm": "^1.0.0",
    "tape": "^4.0.0",
    "xo": "^0.17.0"
  },
  "scripts": {
    "build-md": "remark . --quiet --frail --output",
    "build-bundle": "browserify index.js -s nlcstTest > nlcst-test.js",
    "build-mangle": "esmangle < nlcst-test.js > nlcst-test.min.js",
    "build": "npm run build-md && npm run build-bundle && npm run build-mangle",
    "lint": "xo",
    "test-api": "node test",
    "test-coverage": "nyc --reporter lcov tape test",
    "test": "npm run build && npm run lint && npm run test-coverage"
  },
  "xo": {
    "space": true,
    "rules": {
      "no-eq-null": "off",
      "eqeqeq": "off",
      "guard-for-in": "off"
    },
    "ignores": [
      "nlcst-test.js"
    ]
  },
  "nyc": {
    "check-coverage": true,
    "lines": 100,
    "functions": 100,
    "branches": 100
  },
  "remarkConfig": {
    "presets": "wooorm"
  }
}
